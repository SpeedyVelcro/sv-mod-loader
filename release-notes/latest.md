- Update `README.md` installation instructions such that users don't
  accidentally install gdUnit4 as well.
- Update project icon to a grey/purple colour scheme to fit other SV addons,
- Godot project is now called "SV Mod Loader" instead of "SV Mod Loader Example
  Game" (even though it is the example game) because this looks cleaner in
  Godot's project manager.
- Releases now include an archive with only the addon
- Releases now include .tar.gz versions of archives where applicable to linux
- Release files now include the version number in their filenames.
